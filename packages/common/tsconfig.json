{
  "include": [
    "custom.d.ts",
    "**/*.ts",
    "**/*.tsx"
  ],
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".",
    // Remix takes care of building everything in `remix build`.
    "noEmit": true,
    "paths": {
      "~/*": ["./*"]
    },
  }
}
